import React from 'react';
import Listings from '../containers/Listings';

export const Classifieds = props => {
    return (
        <div className='row'>
            <Listings />
        </div>
    );
};